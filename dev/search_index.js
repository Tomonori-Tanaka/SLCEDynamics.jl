var documenterSearchIndex = {"docs":
[{"category":"section","location":"guide/quantum_thermostat/#The-semi-quantum-thermostat","page":"The semi-quantum thermostat","text":"At low temperature a classical thermostat is qualitatively wrong: equipartition puts k_BT into every magnon mode, so the specific heat stays finite as T to 0 and high-frequency modes are vastly over-occupied. QuantumThermostat implements the semi-quantum colored-noise thermostat of Barker & Bauer, PRB 100, 140401(R) (2019): the thermal-field power spectral density is the classical one times the quantum fluctuation–dissipation factor\n\ntheta(x) = fracxe^x - 1 qquad x = frachbaromegak_B T\n\nwithout the zero-point term. Magnon-mode occupations then follow Bose–Einstein statistics (the specific heat goes to 0 as T to 0) instead of classical equipartition. No zero-point term means T to 0 hands back the deterministic package — the ecosystem's ground-state anchors survive.","title":"The semi-quantum thermostat"},{"category":"section","location":"guide/quantum_thermostat/#Usage","page":"The semi-quantum thermostat","text":"res = run_llg(prob, config0; dt = 1.0, nsteps = 100_000,\n              kT = 0.005, seed = 1,\n              thermostat = QuantumThermostat(),\n              observables = standard_observables(H))\nres.thermostat        # \"quantum\"\n\nThe colored noise is generated by a cascade of biquad filter sections applied to the same per-step white Philox draws the classical thermostat uses — zero extra draws, and a same-seed classical and quantum run share one underlying white realization. At step 0 the filter state is initialized in its exact stationary law (also Philox-keyed), so a run is a pure function of the seed with no burn-in and no carried state before step 1.\n\nThe runtime cost is negligible: zero extra draws per step, and the biquad cascade is arithmetic-light next to the SLCE gradient. On the A100 GPU path the measured quantum-vs-classical step-time overhead at the l044 production sizes was within timing noise at 4³ and +0.4% at 8³ (see GPU and the decision record docs/specs/quantum-thermostat.md Q4) — pick the thermostat on physics, not cost.\n\nA small run, live (8 sites, kT·dt/ħ ≈ 0.008):\n\nusing SLCEDynamics, SLCEMonteCarlo, SLCE\nimport Spglib\nusing LinearAlgebra, Random\n\nlat = Lattice(Matrix(1.0 * I(3)))\ncell = Crystal(lat, reshape([0.0, 0.0, 0.0], 3, 1), [1], [\"Fe\"])\nbasis = SLCEBasis(cell, BasisSpec(; nbody = 2, cutoff = 1.1, lmax = [1],\n                                 soc = false);\n                 backend = SpglibBackend(), images = AllImages())\nmodel = SLCEModel(basis, 0.0, [-0.01])\nH = TiledHamiltonian(model; dims = (2, 2, 2))\nprob = LLGProblem(H; magmom = 2.2, alpha = 0.05)      # α ≲ 0.05: see the caveats\nconfig0 = SLCEMonteCarlo.from_matrix(repeat([0.0, 0.0, 1.0], 1, n_sites(H)))\n\nres = run_llg(prob, config0; dt = 1.0, nsteps = 2000, kT = 0.005, seed = 3,\n              thermostat = QuantumThermostat(),\n              observables = standard_observables(H))\nstats = equilibrium_stats(res; evaluables = Evaluable[])   # raw time averages only\n(thermostat = res.thermostat, energy = stats[:energy].mean[1],\n err = stats[:energy].err[1])","title":"Usage"},{"category":"section","location":"guide/quantum_thermostat/#Validity-window-and-guards","page":"The semi-quantum thermostat","text":"Hard bound: run_llg throws an ArgumentError unless tau = k_BTDelta thbar in 10^-4 01. Above 0.1 the discrete Nyquist band no longer comfortably contains the support of theta — shrink dt for hot runs (the error message names the compliant dt; dt le 005hbark_BT is recommended for ≤ 1% spectral warp at occupied modes). Below approx 10^-4 the discrete sections' DC gain — exactly 1 in closed form — is a cancellation of order alpha_0tau^2 and loses more than 10^-4 relative, so the bound is derived from the shipped coefficients as tau_min = sqrtvarepsilon(alpha_0^minmathrmtol) rather than pinned. Note this is a coefficient-arithmetic limit, not a physical one: a small tau means a fine Delta t, so the Nyquist band x le pitau resolves more of theta(x), not less.\nMarkovian damping: the damping stays local/Markovian (the QTB approximation), so detailed balance is broken; mode occupations are accurate to ~1% only for alpha(hbaromegak_BT) lesssim 003 — keep alpha lesssim 005 for quantum-statistics observables. The classical α-independence gate does not generalize to quantum runs.","title":"Validity window and guards"},{"category":"section","location":"guide/quantum_thermostat/#Statistics-on-quantum-runs","page":"The semi-quantum thermostat","text":"The equilibrium of a QuantumThermostat run is not a Boltzmann ensemble. Fluctuation-formula evaluables (specific heat mathrmvar(E)k_BT^2, susceptibility, Binder — including the standard_evaluables() default) estimate something else there, so equilibrium_stats throws on a quantum run with nonempty evaluables unless you insist with allow_evaluables = true. Pass evaluables = Evaluable[] for the raw time-average stats (always valid), and take response functions from finite differences across runs (e.g. specific heat from dlangle ErangledT).\n\nS(qomega) machinery is trajectory-level and stays valid on quantum runs; the intensities are then semi-quantum. The detailed-balance asymmetry is not reproduced, and a classical→quantum intensity correction factor must never be stacked on top of quantum-thermostat data. The SLCEMonteCarlo Metropolis cross-checks apply only to ClassicalThermostat — a deliberate MC-mismatch tripwire is part of the gate set.","title":"Statistics on quantum runs"},{"category":"section","location":"guide/quantum_thermostat/#Compatibility-and-persistence","page":"The semi-quantum thermostat","text":"Classical mode is untouched: ClassicalThermostat() (the default) is the existing white-noise path, byte-identical to previous package versions for the same seed.\nThe shipped filter matches theta to 0.41% relative over the occupied band (measured certificate: max relative error 4.11e-3 on x in 001 66, max absolute error 7.17e-5 on (66 200, H(0) = 1 exact; full record in docs/specs/quantum-thermostat.md). Its coefficients are trajectory-defining package constants with a provenance tag (_QT_FILTER_ID).\nCheckpoints are refit-proof: schema v3 stores the discrete filter coefficients and the filter state; resume rebuilds the recurrence from the stored coefficients verbatim — never from the current package constants — so a future filter re-fit cannot change a resumed trajectory or brick old files. See checkpointing.\nThe GPU driver takes the same thermostat keyword with the same validation; see GPU.","title":"Compatibility and persistence"},{"category":"section","location":"guide/quantum_thermostat/#A-worked-example","page":"The semi-quantum thermostat","text":"examples/bloch_mt.jl (in the repository) runs a small cubic ferromagnet under both thermostats across temperatures and prints M(T) against linear spin-wave estimates: the classical magnetization deficit is linear in k_BT, while the quantum thermostat freezes out the high-omega magnons at low T — the discrete-grid signature of the Bloch T^32 law. The S(qomega) counterpart (per-mode intensities across a dispersion, gated against the linear-response prediction) is the \"G6\" testset in test/unit/test_quantum_thermostat.jl.","title":"A worked example"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Module","page":"API reference","text":"","title":"Module"},{"category":"section","location":"api/#Units","page":"API reference","text":"","title":"Units"},{"category":"section","location":"api/#Problem-definition","page":"API reference","text":"","title":"Problem definition"},{"category":"section","location":"api/#Integrators","page":"API reference","text":"AbstractIntegrator (public, unexported) is the integrator union Union{DepondtMertens, HeunProjected} — the type of run_llg's integrator keyword and the dispatch seam for future schemes.","title":"Integrators"},{"category":"section","location":"api/#Running","page":"API reference","text":"","title":"Running"},{"category":"section","location":"api/#Thermostats","page":"API reference","text":"AbstractThermostat (public, unexported) is the thermostat union Union{ClassicalThermostat, QuantumThermostat} — the type of the drivers' thermostat keyword.","title":"Thermostats"},{"category":"section","location":"api/#Equilibrium-statistics","page":"API reference","text":"","title":"Equilibrium statistics"},{"category":"section","location":"api/#S(q,ω)","page":"API reference","text":"","title":"S(q,ω)"},{"category":"section","location":"api/#Checkpoint-/-resume","page":"API reference","text":"","title":"Checkpoint / resume"},{"category":"section","location":"api/#GPU","page":"API reference","text":"","title":"GPU"},{"category":"section","location":"api/#Re-exported-from-SLCEMonteCarlo","page":"API reference","text":"Observable, Evaluable, and resume are the same bindings as SLCEMonteCarlo's (re-exported here): observable/evaluable definitions written for the MC drivers plug into this package unchanged, and resume gains the LLGProblem methods documented on this page. See the SLCEMonteCarlo API reference for the Observable/Evaluable contracts.","title":"Re-exported from SLCEMonteCarlo"},{"category":"section","location":"api/#Public,-unexported","page":"API reference","text":"Internal-but-stable machinery is public without being exported — call it qualified (SLCEDynamics.GPULLGState, …).","title":"Public, unexported"},{"category":"module","location":"api/#SLCEDynamics.SLCEDynamics","page":"API reference","text":"Atomistic spin dynamics (Landau–Lifshitz–Gilbert) on fitted spin–lattice cluster-expansion models, on top of SLCEMonteCarlo.jl's tiled Hamiltonian and its exact all-site gradient energy_gradient!.\n\nThe working equation, for unit spin directions e_i with per-site moment magnitude magmom_i (μB units), gyromagnetic `gi, and Gilbert dampingα_i`:\n\nde_i/dt = g_i / (ħ · magmom_i · (1 + α_i²)) · [ e_i × G_i + α_i e_i × (e_i × G_i) ]\n\nwith G_i = ∂E/∂e_i (model energy units, eV for DFT-fitted models) the sum of the SLCE gradient and the Zeeman gradient −magmom_i·μ_B·B_ext. μB enters only at the tesla boundary; the core evolution carries only ħ and the per-site g/magmom. Time is in femtoseconds (`HBAREVFS). The torque convention matches the ecosystem (τi = −ei × Gi = mi × Beff,i, the physical / Landau–Lifshitz torque), and the damping term strictly dissipates:dE/dt = −Σi pi αi |G⊥,i|² ≤ 0`.\n\nReproducibility follows the sibling packages' discipline: the deterministic integrators consume no RNG and are bit-reproducible for any ntasks (SLCEMonteCarlo's P6 scope — same package + Julia version).\n\n\n\n\n\n","title":"SLCEDynamics.SLCEDynamics"},{"category":"constant","location":"api/#SLCEDynamics.HBAR_EV_FS","page":"API reference","text":"HBAR_EV_FS\n\nThe reduced Planck constant ħ in eV·fs, h/(2π·e)·10¹⁵ from the exact SI values of h and e: 0.6582119569509066…. The internal unit system of this package: energies in the model's units (eV), time in fs, so a gradient magnitude |G| [eV] on a moment magmom [μ_B] precesses at g·|G|/(ħ·magmom) [rad/fs].\n\n\n\n\n\n","title":"SLCEDynamics.HBAR_EV_FS"},{"category":"constant","location":"api/#SLCEDynamics.MU_B_EV_T","page":"API reference","text":"MU_B_EV_T\n\nThe Bohr magneton μB in eV/T (CODATA-2018: 5.7883818060e-5). Used only at the tesla boundary — the Zeeman energy `EZ = −Σi magmomi·μB·(ei · Bext)` of an external field in tesla; the core LLG evolution never touches it (μB cancels). A g = 2 spin in B = 1 T precesses at 2·MU_B_EV_T/(2π·HBAR_EV_FS) = 27.9925 GHz — the unit sanity anchor pinned in test_units.jl.\n\n\n\n\n\n","title":"SLCEDynamics.MU_B_EV_T"},{"category":"type","location":"api/#SLCEDynamics.LLGProblem","page":"API reference","text":"LLGProblem(H::TiledHamiltonian; magmom, alpha = 0.0, g = 2.0,\n           b_ext = (0.0, 0.0, 0.0)) -> LLGProblem\n\nDefinition of one LLG dynamics run on the SLCE Hamiltonian H.\n\nmagmom (required): moment magnitudes in μB — a scalar (uniform), a vector of length `nsites(H), or a vector with one entry per training-cell atom (tiled to the supercell viasite_atom`). Must be positive and finite on every active site; inactive sites (no adjacent SLCE instance — outside the fitted magnetic subsystem) are frozen entirely and their entries are ignored.\nalpha: Gilbert damping, scalar or per-site/per-atom vector, ≥ 0.\ng: gyromagnetic g-factor, scalar or per-site/per-atom vector, > 0. With non-uniform g the total moment of a rotation-invariant model is not conserved at α = 0 (each sublattice precesses at its own rate) — expected physics, not an integrator defect.\nb_ext: uniform external field in tesla (3-vector). Enters as the Zeeman energy E_Z = −Σ_i magmom_i·μ_B·(e_i·b_ext) over active sites.\n\nThe equation of motion (unit vectors, G_i = ∂E/∂e_i in model energy units):\n\nde_i/dt = p_i · [e_i × G_i + α_i e_i × (e_i × G_i)],\np_i = g_i / (ħ · magmom_i · (1 + α_i²))    [1/(eV·fs)]\n\nInactive sites have p_i = 0 and are additionally skipped bitwise by the integrators (mirroring SLCEMonteCarlo's frozen-spin convention).\n\n\n\n\n\n","title":"SLCEDynamics.LLGProblem"},{"category":"method","location":"api/#SLCEMonteCarlo.total_energy-Tuple{LLGProblem, Vector{StaticArraysCore.SVector{3, Float64}}}","page":"API reference","text":"SLCEMonteCarlo.total_energy(prob::LLGProblem, config::SpinConfig) -> Float64\n\nThe dynamical energy of config: the SLCE energy (total_energy(prob.H, config), intercept excluded) plus the Zeeman energy −Σ_i magmom_i·μ_B·(e_i·b_ext) over active sites. This is the conserved quantity at α = 0 and the monotonically decreasing one at α > 0 (with T = 0).\n\n\n\n\n\n","title":"SLCEMonteCarlo.total_energy"},{"category":"type","location":"api/#SLCEDynamics.DepondtMertens","page":"API reference","text":"DepondtMertens()\n\nRotation-based Heun integrator (Depondt & Mertens, J. Phys.: Condens. Matter 21, 336005 (2009)): predictor and corrector each advance every spin by a rigid Rodrigues rotation about its local rotation vector ω_s, so |e_s| = 1 is preserved to rounding with no renormalization step. The corrector rotates the original spin about the stage-averaged (ω¹ + ω²)/2. Weak/deterministic order 2 in dt; exact for a constant rotation vector (pure Zeeman precession). The recommended default.\n\n\n\n\n\n","title":"SLCEDynamics.DepondtMertens"},{"category":"type","location":"api/#SLCEDynamics.HeunProjected","page":"API reference","text":"HeunProjected()\n\nClassical explicit Heun with per-stage renormalization (the standard reference scheme of the stochastic-LLG literature, e.g. García-Palacios & Lázaro, PRB 58, 14937 (1998)): Euler predictor along de/dt = ω × e, trapezoidal corrector, each followed by e → e/|e|. Deterministic order 2. Kept as an independent cross-check of DepondtMertens — two structurally different integrators agreeing to O(dt²) is a strong implementation gate.\n\n\n\n\n\n","title":"SLCEDynamics.HeunProjected"},{"category":"function","location":"api/#SLCEDynamics.run_llg","page":"API reference","text":"run_llg(prob::LLGProblem, config0::SpinConfig; dt, nsteps,\n        integrator = DepondtMertens(), observables = Observable[],\n        temperature = nothing, kT = nothing, seed = nothing,\n        thermostat = ClassicalThermostat(), measure_interval = 10,\n        ntasks = 1, renorm_interval = 1000,\n        checkpoint = nothing, checkpoint_interval = 0)\n    -> LLGResult\n\nIntegrate the LLG for nsteps fixed steps of dt [fs] from config0 (unit vectors; not mutated) — deterministic (T = 0) by default, stochastic LLG when a temperature is given.\n\nEach active column of config0 passes the family's unit-direction door (SLCE.UnitVector3): it must be finite, within 1e-6 of unit norm, and is then projected exactly onto the sphere before the first step — so a direction carrying float noise (a rounded file, an off-by-5e-9 near-pole column that would otherwise throw a DomainError from inside the gradient's Legendre recursion) is repaired at the door instead of failing mid-run. Inactive sites' entries are unvalidated placeholders and pass through verbatim (they stay bitwise frozen). Observables are recorded at step 0, every measure_interval steps, and always at the final step (so the last measurement matches the returned config exactly).\n\ntemperature [K] XOR kT [model energy units] (scalar; both omitted = deterministic): switches on the thermal field G_th,i = σ_i·ξ_i with σ_i = √(2 α_i kB T ħ magmom_i/(g_i Δt)) — the fluctuation–dissipation amplitude for this parametrization (no (1+α²); see noise.jl). Requires α > 0 on every active site (noise scales with the damping; an undamped spin never thermalizes). One Gaussian 3-vector per active site per step, shared by both integrator stages (Stratonovich). To feed equilibrium_stats's default evaluables (specific heat &c.), record SLCEMonteCarlo.standard_observables(prob.H) here.\nseed: the noise seed (thermostatted runs only; default: a fresh rand(UInt64), recorded in the result). Draws are keyed counter-based Philox — the trajectory is a pure function of (prob, config0, dt, nsteps, integrator, seed), no RNG state exists, and results are bit-identical for any ntasks.\nthermostat: ClassicalThermostat() (default — white noise, the classical FDT) or QuantumThermostat() (semi-quantum colored noise; requires a temperature and kT·dt/ħ ≤ 0.1 — see the type's docstring for the physics and its validity limits). The quantum filter consumes the SAME white draws, so a same-seed classical and quantum run share one underlying white realization.\nintegrator: DepondtMertens (default) or HeunProjected.\nobservables: a vector of SLCEMonteCarlo.Observables — the SAME definitions the Monte Carlo drivers accept (Observable(name, ncomp, f) with f(v::SLCEMonteCarlo.MCView), including SLCEMonteCarlo.standard_observables(H) and any user-defined ones). Per the Observable contract, v.energy is the SLCE energy (model units, intercept excluded, Zeeman not included) so a definition measures identical values on the same configuration in both packages. v.disps is empty here — spin dynamics has no displacement channel — so a displacement observable throws rather than reporting zeros. Each observable's time series lands in LLGResult.series[name] as an ncomp × n_measurements matrix. Names must be unique.\nntasks: task count for the two per-step field evaluations (SLCEMonteCarlo.energy_gradient!) — bit-identical results for any value.\nrenorm_interval: every that many steps, active spins are renormalized to unit length (0 disables). Depondt–Mertens preserves norms to rounding, so this only caps the ~ε√nsteps rounding walk of very long runs; HeunProjected renormalizes every step by construction.\ncheckpoint: a JLD2 path for crash-safe restart files (nothing disables). With a path, a checkpoint is written every checkpoint_interval steps (0 ⇒ only at completion) and always once at completion; continue or reload with resume. The noise is a stateless pure function of (seed, site, step), so the file carries no RNG state (a quantum thermostat's filter state is the one carried array, stored verbatim) — a resumed run is bit-identical to the uninterrupted one. Writes are atomic (temp file + mv).\n\nA deterministic run consumes no RNG at all. Inactive sites stay bitwise frozen throughout (no noise, no Zeeman).\n\n\n\n\n\n","title":"SLCEDynamics.run_llg"},{"category":"type","location":"api/#SLCEDynamics.LLGResult","page":"API reference","text":"LLGResult\n\nResult of one run_llg. Measurements are taken at step 0, every measure_interval steps, and always at the final step, so times[end] == nsteps·dt and the last measurement describes exactly the returned config. Fields:\n\ntimes [fs], energies — the dynamical energy (SLCE + Zeeman),\nmean_spins — the unweighted mean direction over active sites (the SLCEMonteCarlo magnetization convention),\nseries::Dict{Symbol,Matrix{Float64}} — one ncomp × n_measurements matrix per user observable (see run_llg's observables), columns aligned with times,\nconfig — the final configuration, plus the run parameters. For a thermostatted run, kT [eV] and the seed are recorded (kT = NaN, seed = 0 for a deterministic run); b_ext [T] is the problem's uniform external field — equilibrium_stats needs it because the recorded :energy observable series is the SLCE energy alone, which at b_ext ≠ 0 is not the energy the Boltzmann weight uses; n_active is the active-site count and n_spin_active the magnetic-site count — an Evaluable's scope picks which one it is normalized by in equilibrium_stats, and the two differ exactly when a joint spin–lattice Hamiltonian carries displacement-only sites; compute is the provenance tag (\"cpu\", or \"gpu:<backend>\" from gpu_run_llg — GPU trajectories are bit-reproducible only for a fixed backend and workgroup size); thermostat is \"classical\" (white noise — also for deterministic runs) or \"quantum\" (the colored-noise QuantumThermostat, whose equilibrium is NOT a Boltzmann ensemble — see equilibrium_stats).\n\n\n\n\n\n","title":"SLCEDynamics.LLGResult"},{"category":"type","location":"api/#SLCEDynamics.ClassicalThermostat","page":"API reference","text":"ClassicalThermostat()\n\nWhite thermal noise (the classical fluctuation–dissipation theorem) — the existing stochastic-LLG path, byte-identical to previous package versions for the same seed. The default thermostat of run_llg.\n\n\n\n\n\n","title":"SLCEDynamics.ClassicalThermostat"},{"category":"type","location":"api/#SLCEDynamics.QuantumThermostat","page":"API reference","text":"QuantumThermostat()\n\nSemi-quantum colored-noise thermostat (Barker & Bauer, PRB 100, 140401(R) (2019)): the thermal-field power spectral density is the classical one times θ(x) = x/(eˣ − 1), x = ħ|ω|/kT — the quantum fluctuation–dissipation factor without the zero-point term — so magnon-mode occupations follow Bose–Einstein statistics (specific heat → 0 as T → 0) instead of classical equipartition. Requires a temperature and kT·dt/ħ ≤ 0.1 (shrink dt for hot runs; dt ≤ 0.05·ħ/kT recommended).\n\nAccuracy notes (see SPEC.md): the damping stays Markovian, so mode occupations are accurate to ~1% only for α·(ħω/kT) ≲ 0.03 — keep α ≲ 0.05 for quantum-statistics observables. Equilibrium is not a Boltzmann ensemble: fluctuation-formula evaluables are refused by equilibrium_stats (take specific heat from d⟨E⟩/dT across runs instead), and the classical MC cross-checks apply only to ClassicalThermostat.\n\nThe shipped filter (_QT_S_BIQUADS, fitted by dev/fit_qtb_filter.jl) matches θ to 0.41% relative over the occupied band — see docs/specs/quantum-thermostat.md for the full certificate. The filter coefficients are trajectory-defining package constants (_QT_FILTER_ID); checkpoints store them and resume from the stored values verbatim.\n\n\n\n\n\n","title":"SLCEDynamics.QuantumThermostat"},{"category":"function","location":"api/#SLCEDynamics.equilibrium_stats","page":"API reference","text":"equilibrium_stats(result::LLGResult;\n                  evaluables = standard_evaluables(),\n                  discard = length(result.times) ÷ 2,\n                  nbins = 32, allow_evaluables = false)\n    -> Dict{Symbol,ObservableStat}\n\nLong-time (equilibrium) averages of a thermostatted run_llg result: every recorded observable series becomes an ObservableStat (autocorrelation-aware mean/error/τint via SLCEMonteCarlo.LogBinner), and each Evaluable — the same definitions the MC drivers accept, e.g. `SLCEMonteCarlo.standardevaluables()— is jackknifed over equal-weight bins of its (scalar) input observables, withf(means, kT, n)receiving the run'skTand the site count its **scope** declares:nactiveforscope = :energy,nspinactiveotherwise. The two coincide on a pure-spin model and diverge exactly where a joint model has displacement-only sites, so a:spin-scoped evaluable written against \"the active site count\" is wrong bynactive / nspinactive` the first time it meets one.\n\ndiscard: number of leading measurements dropped as thermalization (default: the first half). The sLLG equilibration time scales like 1/α — inspect the energy series when in doubt.\nnbins: jackknife bin count (a trailing remainder is dropped, as in the MC drivers). An evaluable needs every input among the recorded observables (else: ArgumentError — pass them to run_llg's observables, e.g. SLCEMonteCarlo.standard_observables(H)) and ≥ 2 completed bins (else its stat is NaN).\n\nStatistical caveat (SPEC.md): the Heun-family sLLG has weak order 1, so equilibrium averages carry an O(dt) bias — check dt-sensitivity before trusting a tight comparison. A deterministic run has no ensemble; calling this on one is an error.\n\nQuantum-thermostat runs: the equilibrium of a QuantumThermostat run is NOT a Boltzmann ensemble, so fluctuation-formula evaluables (specific heat var(E)/kT², susceptibility, Binder — including the standard_evaluables() default) estimate something else there and are refused unless allow_evaluables = true. Pass evaluables = Evaluable[] for the raw time-average stats (always valid), or take response functions from finite differences across runs (e.g. specific heat from d⟨E⟩/dT).\n\nRuns with an external field: at b_ext ≠ 0 the equilibrium measure is exp(−(E_SCE + E_Zeeman)/kT), but the recorded :energy observable series — the only thing the evaluables read — is the SLCE energy alone (the SLCEMonteCarlo observable contract), so var(E)/kT² would silently omit var(E_Z) + 2·cov(E_SCE, E_Z) (≈ all of the specific heat for a weakly anisotropic spin in a strong field), and the zero-field susceptibility formula is not ∂m/∂B at finite field. Evaluables are therefore refused on such a run unless allow_evaluables = true; the raw time-average stats (evaluables = Evaluable[]) remain valid, and result.energies (which does include the Zeeman term) is available for hand-built estimates.\n\n\n\n\n\n","title":"SLCEDynamics.equilibrium_stats"},{"category":"function","location":"api/#SLCEDynamics.trajectory_observable","page":"API reference","text":"trajectory_observable(H::TiledHamiltonian; name = :spins) -> Observable\n\nAn observable recording the full spin configuration (ncomp = 3·n_sites(H), column-major [x₁,y₁,z₁, x₂,…] = vec(to_matrix(config))). Feed it to run_llg's observables to acquire the trajectory that structure_factor consumes; the series is persisted by the checkpoint machinery like any other observable (note the file-size cost: 24 bytes per site per measurement). Memory guidance: the in-RAM trajectory is 24·n_sites·n_meas bytes — keep it ≲ 10 GB (e.g. 8³ cells × 68 atoms × 4096 frames ≈ 3.4 GB).\n\n\n\n\n\n","title":"SLCEDynamics.trajectory_observable"},{"category":"function","location":"api/#SLCEDynamics.trajectory","page":"API reference","text":"trajectory(result::LLGResult; name = :spins)\n    -> (; traj::AbstractArray{Float64,3}, times::Vector{Float64})\n\nThe recorded trajectory as a zero-copy 3 × n_sites × n_meas reshape of result.series[name], with its time axis. When the run's final measurement was off the measure_interval grid (nsteps % measure_interval ≠ 0), that last column is dropped — the S(q,ω) estimator needs a strictly uniform time grid.\n\n\n\n\n\n","title":"SLCEDynamics.trajectory"},{"category":"function","location":"api/#SLCEDynamics.q_path","page":"API reference","text":"q_path(crystal::Crystal, vertices; npoints = 100, dims = nothing)\n    -> (; qs, qs_requested, x, vert_idx)\n\nFractional q points (training-cell r.l.u.) along the piecewise-linear path through vertices, distributed proportionally to Cartesian segment length. x is the cumulative Cartesian path length [rad/Å] of the returned points (the plot abscissa) and vert_idx the indices of the path vertices among them.\n\nWith dims (pass H.dims), every point is snapped to the supercell's commensurate grid (round(fᵢNᵢ)/Nᵢ per component; exact half-grid points follow round's ties-to-even) and consecutive duplicates are merged — qs holds the snapped points, qs_requested the pre-snap ones, so the snapping is always visible. Without dims, no snapping happens (and structure_factor will throw on incommensurate points — by design).\n\n\n\n\n\n","title":"SLCEDynamics.q_path"},{"category":"function","location":"api/#SLCEDynamics.structure_factor","page":"API reference","text":"structure_factor(traj, times, H::TiledHamiltonian, crystal::Crystal, qs;\n                 nsegments = 1, overlap = 0.5, window = :hann, discard = 0,\n                 seglength = nothing, ntasks = 1) -> SQWResult\n\nThe dynamical structure factor of a spin trajectory (3 × n_sites × n_meas, uniform time grid times [fs]) at the fractional wavevectors qs (training-cell r.l.u.; every q must be commensurate with the supercell — q[i]·dims[i] ∈ ℤ — or an ArgumentError names the nearest representable point; snap with q_path). crystal is the training cell H was tiled from.\n\nEstimator (see SPEC.md for the settled conventions): per-site time means over the analysis window are always subtracted (the elastic tensor is reported separately in S_el); Welch averaging over nsegments segments of derived power-of-two length nfft (seglength overrides; trailing samples are truncated, never zero-padded) with fractional overlap and a :hann (default) or :none window; discard leading measurements are dropped as thermalization. The result is bit-identical for any ntasks and across repeated calls.\n\nConvenience methods: structure_factor(result::LLGResult, H, crystal, qs; name = :spins, …) (records via trajectory_observable; the off-grid final measurement, if any, is dropped), structure_factor(::Vector{LLGResult}, …) (a seed ensemble: spectra averaged, realization standard errors in err for ≥ 3 members), and structure_factor(path::AbstractString, …) (a run_llg checkpoint file — the persisted trajectory format).\n\nwarning: Temporal aliasing\nThe analysis Nyquist is π/dt_meas = π/(measure_interval·dt) — measure_interval times LOWER than the integration Nyquist. Spectral content above it folds back into the band as a spurious branch, and every sum rule still passes (aliasing conserves power), so nothing downstream can detect it. Choose measure_interval so the highest mode sits well below π/dt_meas. As a screen, a warning fires when more than 10.0 % of the inelastic weight AT ANY SINGLE q (worst q wins — a fold confined to part of a long q-path would be diluted away in an all-q aggregate) sits within the top 5.0 % of the frequency range — a band top crossing the Nyquist folds back continuously, so it necessarily deposits weight at the edge. (With few frequency bins the threshold floors at twice the edge band's actual bin fraction, which is what a structureless spectrum puts there.) The screen is a heuristic in one direction only: a DEEPLY folded isolated mode lands mid-spectrum where it is indistinguishable from a real branch, so the absence of the warning is not a certificate.\n\n\n\n\n\n","title":"SLCEDynamics.structure_factor"},{"category":"type","location":"api/#SLCEDynamics.SQWResult","page":"API reference","text":"SQWResult\n\nDynamical structure factor on a q-list × two-sided frequency grid. Fields:\n\nqs — the fractional q (training-cell r.l.u.), qs_requested the pre-snap inputs (identical unless a snapping path helper produced them), qs_cart the Cartesian wavevectors [rad/Å],\nomegas [rad/fs, ascending, two-sided; for even nfft the Nyquist bin exists only on the negative side] and the convenience axis energies_mev = ħω [meV],\nS[α, β, iq, iω] — the full Hermitian tensor (diagonals real ≥ 0); use the reductions sqw_diag, sqw_trace, sqw_perp, sqw_plusminus,\nS_el[α, β, iq] — the elastic (per-site-time-mean) tensor, separated so Bragg weight never leaks into the inelastic spectrum,\nerr — per-element standard error over independent realizations (seed ensembles with ≥ 3 members; nothing otherwise). For the complex off-diagonals this is the combined real+imag scatter √(Σ|S_r − S̄|²/(R(R−1))) — one real number per element,\nthe estimation metadata (window, nfft, nsegments, overlap, discard, dt_meas [fs], nrealizations).\n\nNormalization: with window = :none and one segment, Σ_k S^{αα}(q,ω_k)/(nfft·dt_meas) = ⟨|s^α(q,t)|²⟩ exactly (Parseval), i.e. the ω-integral ∫S dω/2π recovers the equal-time fluctuation correlator.\n\n\n\n\n\n","title":"SLCEDynamics.SQWResult"},{"category":"function","location":"api/#SLCEDynamics.sqw_diag","page":"API reference","text":"sqw_diag(r::SQWResult) -> Array{Float64,3}\n\nThe three diagonal components S^{xx}, S^{yy}, S^{zz} as out[α, iq, iω].\n\n\n\n\n\n","title":"SLCEDynamics.sqw_diag"},{"category":"function","location":"api/#SLCEDynamics.sqw_trace","page":"API reference","text":"sqw_trace(r::SQWResult) -> Matrix{Float64}\n\nΣ_α S^{αα} as out[iq, iω] — the rotation-invariant total intensity (the sum-rule object).\n\n\n\n\n\n","title":"SLCEDynamics.sqw_trace"},{"category":"function","location":"api/#SLCEDynamics.sqw_perp","page":"API reference","text":"sqw_perp(r::SQWResult) -> Matrix{Float64}\n\nThe unpolarized-neutron weight Σ_{αβ}(δ_{αβ} − q̂_α q̂_β) S^{αβ} as out[iq, iω], with q̂ the physical Cartesian direction (including any reciprocal-lattice offset). At q = 0 the direction is undefined — that row is NaN (no silent convention).\n\n\n\n\n\n","title":"SLCEDynamics.sqw_perp"},{"category":"function","location":"api/#SLCEDynamics.sqw_plusminus","page":"API reference","text":"sqw_plusminus(r::SQWResult; axis = SVector(0.0, 0.0, 1.0)) -> Matrix{Float64}\n\nThe transverse spectrum S^{+−} = S_uu + S_vv − 2·Im S_uv (e^± = e_u ± i e_v in a right-handed frame (u, v, axis)) as out[iq, iω], real ≥ 0. A spin precessing positively about +axis contributes at positive ω — this is the component that resolves the magnon circulation sense. The default axis ẑ uses exactly u = x̂, v = ŷ.\n\n\n\n\n\n","title":"SLCEDynamics.sqw_plusminus"},{"category":"function","location":"api/#SLCEDynamics.sqw_elastic","page":"API reference","text":"sqw_elastic(r::SQWResult) -> Array{ComplexF64,3}\n\nThe elastic tensor S_el[α, β, iq] (the S_el field).\n\n\n\n\n\n","title":"SLCEDynamics.sqw_elastic"},{"category":"function","location":"api/#SLCEDynamics.homega_ev","page":"API reference","text":"ħω axis in eV (HBAR_EV_FS .* omegas).\n\n\n\n\n\n","title":"SLCEDynamics.homega_ev"},{"category":"function","location":"api/#SLCEDynamics.homega_mev","page":"API reference","text":"ħω axis in meV (the energies_mev field, recomputed).\n\n\n\n\n\n","title":"SLCEDynamics.homega_mev"},{"category":"function","location":"api/#SLCEDynamics.freq_thz","page":"API reference","text":"Frequency axis in THz (1000·ω/2π).\n\n\n\n\n\n","title":"SLCEDynamics.freq_thz"},{"category":"method","location":"api/#SLCEMonteCarlo.resume-Tuple{AbstractString, LLGProblem}","page":"API reference","text":"resume(path, prob::LLGProblem;\n       observables = Observable[], nsteps = nothing, ntasks = 1,\n       checkpoint = path, checkpoint_interval = nothing) -> LLGResult\n\nContinue a checkpointed run_llg from the state saved at path and return the full run's result — bit-identical to the uninterrupted run (the thermal noise is a stateless function of (seed, site, step), so no RNG state exists to restore; a quantum run's filter state is restored verbatim). Calling it on the checkpoint of a completed run reconstructs that run's LLGResult without stepping (idempotent — safe in a job-array retry loop). A checkpoint written by gpu_run_llg is refused here — resume it with the resume(path, prob, gH) method.\n\nThe caller re-supplies the LLGProblem and the observable functions (closures are not serialized); the checkpoint stores the model fingerprint, the problem parameters (magmom/alpha/g/b_ext, compared on active sites), and the observable names/component counts, and errors on any mismatch. Everything trajectory-defining (dt, measure_interval, renorm_interval, integrator, kT, seed, thermostat) comes from the file and cannot be overridden — a quantum-thermostat run's filter is rebuilt from the STORED discrete coefficients verbatim (never from the current package constants, so a future constants re-fit cannot change a resumed trajectory) and its filter state is restored bitwise alongside the configuration.\n\nnsteps: target total step count (default: the stored one). A value larger than the stored nsteps extends the run — bit-identical to a single uninterrupted run of that length. Extension past a completed run whose final measurement was off the measure_interval grid is refused (its recorded trace would not be a prefix of the longer run's).\ncheckpoint / checkpoint_interval: by default the resumed run keeps checkpointing to the same path with the stored cadence (checkpoint = nothing disables; checkpoint_interval overrides).\n\n\n\n\n\n","title":"SLCEMonteCarlo.resume"},{"category":"function","location":"api/#SLCEDynamics.gpu_run_llg","page":"API reference","text":"gpu_run_llg(prob::LLGProblem, config0::SpinConfig, gH;\n            dt, nsteps, integrator = DepondtMertens(),\n            observables = Observable[], temperature = nothing, kT = nothing,\n            seed = nothing, thermostat = ClassicalThermostat(),\n            measure_interval = 10, renorm_interval = 1000,\n            workgroupsize = 128, checkpoint = nothing,\n            checkpoint_interval = 0) -> LLGResult\n\nrun_llg on a KernelAbstractions device: the same physics, observables (host-evaluated on a per-measurement snapshot — the identical Observable contract and LLGResult semantics, so equilibrium_stats / structure_factor / checkpointing consume the result unchanged), the same config0 entry door (active columns validated and projected onto the sphere — see run_llg), and stateless Philox noise stream (same (seed, site, step) draws as the CPU path — a same-seed CPU and GPU run are the same stochastic realization, differing only through gradient/rotation roundoff).\n\ngH is SLCEMonteCarlo.GPUTiledHamiltonian(backend, prob.H) — build it once and reuse it across runs (the table upload is seconds at production sizes).\n\nthermostat accepts QuantumThermostat() with the same contract as run_llg (temperature required, kT·dt/ħ ≤ 0.1): the stationary init always runs on the host and is uploaded once; the in-kernel cascade filters the same white draws as the CPU path.\n\nDeterminism scope: the trajectory is bitwise reproducible for a fixed (seed, backend, workgroupsize, package + Julia version) — the gradient fold order depends on workgroupsize (pinned default 128), so unlike the CPU path it is part of the contract, and CPU↔GPU trajectories are never bitwise equal. The result's compute field and the checkpoint record all three.\n\n\n\n\n\n","title":"SLCEDynamics.gpu_run_llg"},{"category":"method","location":"api/#SLCEMonteCarlo.resume-Tuple{AbstractString, LLGProblem, Any}","page":"API reference","text":"resume(path, prob::LLGProblem, gH;\n       observables = Observable[], nsteps = nothing,\n       allow_compute_switch = false, workgroupsize = nothing,\n       checkpoint = path, checkpoint_interval = nothing) -> LLGResult\n\nContinue a checkpointed run on the device gH was built for. For a checkpoint written by gpu_run_llg on the SAME backend and workgroup size, the continuation (and any nsteps extension) is bit-identical to the uninterrupted device run.\n\nAny compute switch — resuming a CPU file here, a GPU file with a different backend tag, or a different workgroupsize — is refused unless allow_compute_switch = true. When allowed, the continuation restores the configuration verbatim and the same (seed, site, step) noise stream, so it is the SAME physical realization continued exactly from a valid state (statistically impeccable) — but it is not bit-identical to any single-backend run; the switch must be a deliberate, loud decision (a silent backend change once cost a walltime — see the GPU decision records).\n\n\n\n\n\n","title":"SLCEMonteCarlo.resume"},{"category":"type","location":"api/#SLCEDynamics.GPULLGState","page":"API reference","text":"GPULLGState(gH::GPUTiledHamiltonian, prob::LLGProblem, config0::SpinConfig,\n            sigma::Vector{Float64}, fstate = nothing) -> GPULLGState\n\nDevice arrays for one gpu_run_llg: the configuration and the integrator scratch (depred/domega1/dG/dgth — the device sibling of _LLGScratch), the per-site problem parameters uploaded once (dgzee/dpref/dalpha/dsigma/dactive), the upstream gradient scratch, and a preallocated host snapshot buffer for measurement/checkpoint downloads.\n\nWith a quantum-thermostat fstate::_FilterState, the filter state lives on the device as dxstate — the n × (6·NS) TRANSPOSE of the host (6·NS) × n layout (site index fastest per lane — coalesced for the one-thread-per-site noise kernel); the transpose happens only at the event-gated uploads/downloads (a bitwise-lossless permutation, staged through h_xstage). Classical runs carry empty (0-sized) filter arrays.\n\n\n\n\n\n","title":"SLCEDynamics.GPULLGState"},{"category":"type","location":"api/#SLCEDynamics.ColoredNoiseFilter","page":"API reference","text":"ColoredNoiseFilter(sections::Vector{_Biquad}, L::Matrix{Float64})\n\nThe discrete-time colored-noise generator of the quantum thermostat: a cascade of biquad sections applied to the per-step white draws, plus the stationary square root L (L·Lᵀ = the stationary state covariance, used ONLY by the step-0 initialization — never persisted, never touched by the stepping loop). Built by _build_quantum_filter(kt, dt); user construction is not part of the API (the coefficients are trajectory-defining and ship as package constants).\n\n\n\n\n\n","title":"SLCEDynamics.ColoredNoiseFilter"},{"category":"function","location":"api/#SLCEDynamics.channel_sumrule","page":"API reference","text":"channel_sumrule(traj, times, H::TiledHamiltonian, crystal::Crystal)\n    -> (; lhs, rhs)\n\nThe global Parseval identity of the estimator, at the sublattice-channel level (where it is exact — the unfolded S is not periodic in one supercell BZ): lhs = Σ_{q ∈ grid} Σ_{a,α} Σ_k S_aa^{αα}(q,ω_k)/(M·Δt) over the full commensurate grid, computed through the FFT pipeline (window = :none, one segment, M = prevpow(2, n_meas)), and rhs = Σ_{s active} (1 − |ē_s|²) computed directly. They agree to ~1e-10·N for any trajectory — a plumbing gate, not a physics assumption (unit spins enter only via |e| = 1).\n\n\n\n\n\n","title":"SLCEDynamics.channel_sumrule"},{"category":"section","location":"guide/dynamics/#Deterministic-LLG","page":"Deterministic LLG","text":"run_llg integrates the Landau–Lifshitz–Gilbert equation for unit spin directions mathbf e_i on the SLCE Hamiltonian, with fixed step dt [fs]:\n\nfracdmathbf e_idt = p_ibiglmathbf e_i times mathbf G_i\n    + alpha_i mathbf e_i times (mathbf e_i times mathbf G_i)bigr\nqquad\np_i = fracg_ihbar mathrmmagmom_i(1 + alpha_i^2)\n\nwhere mathbf G_i = partial Epartial mathbf e_i (model energy units) is the sum of the exact SLCE gradient (SLCEMonteCarlo.energy_gradient!) and the Zeeman gradient -mathrmmagmom_imu_Bmathbf B_mathrmext. The torque convention matches the ecosystem (boldsymboltau_i = -mathbf e_i times mathbf G_i, the physical / Landau–Lifshitz torque), and the damping term strictly dissipates: dEdt = -sum_i p_i alpha_i mathbf G_perpi^2 le 0, exactly zero at alpha = 0.","title":"Deterministic LLG"},{"category":"section","location":"guide/dynamics/#Units","page":"Deterministic LLG","text":"Energy: the model's units — eV for DFT-fitted models. Time: fs. The two meet in HBAR_EV_FS (ħ in eV·fs, an exact SI ratio): a gradient magnitude G [eV] on a moment magmom [μ_B] precesses at gG(hbarmathrmmagmom) [rad/fs].\nField: tesla, only at the boundary — MU_B_EV_T converts b_ext into the Zeeman energy E_Z = -sum_i mathrmmagmom_imu_B(mathbf e_i cdot mathbf B_mathrmext); the core evolution never touches μ_B (it cancels). The pinned sanity anchor: a g = 2 spin in 1 T precesses at 27.9925 GHz.","title":"Units"},{"category":"section","location":"guide/dynamics/#The-problem-definition","page":"Deterministic LLG","text":"LLGProblem(H; magmom, alpha = 0.0, g = 2.0, b_ext = (0, 0, 0)) holds the Hamiltonian plus the per-site material parameters. magmom (μ_B, required), alpha (≥ 0), and g (> 0) each accept a scalar, a vector with one entry per training-cell atom (tiled to the supercell), or a full per-site vector. Two conventions inherited from the siblings:\n\nInactive sites (outside the fitted magnetic subsystem) are frozen bitwise — skipped by both integrator stages, no noise, no Zeeman, excluded from mean_spins; their parameter entries are ignored.\nWith non-uniform g, the total moment of a rotation-invariant model is not conserved at alpha = 0 (each sublattice precesses at its own rate) — expected physics, not an integrator defect.","title":"The problem definition"},{"category":"section","location":"guide/dynamics/#Integrators","page":"Deterministic LLG","text":"DepondtMertens (the default) — rotation-based Heun: each stage advances every spin by a rigid Rodrigues rotation about its local rotation vector, so mathbf e_i = 1 is preserved to rounding with no renormalization. It is exact for a constant rotation vector, so uniform Larmor precession in a pure external field is reproduced to rounding at any step size.\nHeunProjected — classical explicit Heun with per-stage renormalization, the standard reference scheme of the stochastic-LLG literature. Kept as an independent cross-check: two structurally different integrators agreeing to O(dt^2) is a strong implementation gate.\n\nBoth are deterministic order 2 and cost two field evaluations per step (≈ two Metropolis sweeps of the same Hamiltonian).","title":"Integrators"},{"category":"section","location":"guide/dynamics/#Choosing-dt","page":"Deterministic LLG","text":"Resolve the fastest precession: the largest p_i mathbf G_i in the run should satisfy omega_mathrmmax dt ll 1 (a few 0.01–0.1 rad per step). Strongly coupled models (larger gradients) or small moments (larger p) need smaller steps; check convergence by halving dt — the deterministic error contracts at O(dt^2). For thermostatted runs see the additional considerations in thermal and quantum thermostat.","title":"Choosing dt"},{"category":"section","location":"guide/dynamics/#Measurement-and-renormalization-cadence","page":"Deterministic LLG","text":"Measurements are taken at step 0, every measure_interval steps, and always at the final step, so times[end] == nsteps·dt and the last measurement describes exactly the returned config. renorm_interval (default 1000) renormalizes active spins to unit length every that many steps: Depondt–Mertens preserves norms to rounding, so this only caps the sim varepsilonsqrtn_mathrmsteps rounding walk of very long runs (0 disables; HeunProjected renormalizes every step by construction).","title":"Measurement and renormalization cadence"},{"category":"section","location":"guide/dynamics/#Observables-and-evaluables","page":"Deterministic LLG","text":"Nothing is hard-coded into the stepping loop: run_llg records a vector of SLCEMonteCarlo.Observables — the same definitions the MC drivers accept (Observable(name, ncomp, f) with f(v::SLCEMonteCarlo.MCView)), including SLCEMonteCarlo.standard_observables(H) and user-defined ones. Per the Observable contract, energy is the SLCE energy (model units, intercept excluded, Zeeman not included), so a definition measures identical values on the same configuration in both packages. Each series lands in LLGResult.series[name] as an ncomp × n_measurements matrix; Evaluables enter downstream, through equilibrium_stats on thermostatted runs.","title":"Observables and evaluables"},{"category":"section","location":"guide/dynamics/#Energy-conservation-at-\\alpha-0","page":"Deterministic LLG","text":"At zero damping the dynamical energy (SLCE + Zeeman) is conserved; the fixed-step integrator leaves an O(dt^2) drift. On the cubic ferromagnet of Getting started:\n\nusing SLCEDynamics, SLCEMonteCarlo, SLCE\nimport Spglib\nusing LinearAlgebra, Random\n\nlat = Lattice(Matrix(1.0 * I(3)))\ncell = Crystal(lat, reshape([0.0, 0.0, 0.0], 3, 1), [1], [\"Fe\"])\nbasis = SLCEBasis(cell, BasisSpec(; nbody = 2, cutoff = 1.1, lmax = [1],\n                                 soc = false);\n                 backend = SpglibBackend(), images = AllImages())\nmodel = SLCEModel(basis, 0.0, [-0.01])\nH = TiledHamiltonian(model; dims = (2, 2, 2))\n\nm0 = randn(Xoshiro(2), 3, n_sites(H))\nconfig0 = SLCEMonteCarlo.from_matrix(m0 ./ sqrt.(sum(abs2, m0; dims = 1)))\ncons = run_llg(LLGProblem(H; magmom = 2.2, alpha = 0.0), config0;\n               dt = 0.25, nsteps = 800, measure_interval = 10)\nmaximum(abs, cons.energies .- cons.energies[1])   # the α = 0 conservation gate\n\nWith damping the same start dissipates monotonically (compare the largest recorded energy rise against the total drop):\n\ndiss = run_llg(LLGProblem(H; magmom = 2.2, alpha = 0.5), config0;\n               dt = 0.25, nsteps = 800, measure_interval = 10)\n(drop = diss.energies[end] - diss.energies[1],\n max_rise = maximum(diff(diss.energies)))\n\nBoth runs are RNG-free and bit-identical for any ntasks — rerunning either reproduces the arrays exactly.","title":"Energy conservation at alpha = 0"},{"category":"section","location":"getting_started/#Getting-started","page":"Getting started","text":"","title":"Getting started"},{"category":"section","location":"getting_started/#Install","page":"Getting started","text":"The package lives alongside its model source SLCE.jl and its Hamiltonian / gradient layer SLCEMonteCarlo.jl; during development all three are path-devs:\n\nusing Pkg\nPkg.develop(path = \"path/to/SLCE.jl\")\nPkg.develop(path = \"path/to/SLCEMonteCarlo.jl\")\nPkg.develop(path = \"path/to/SLCEDynamics.jl\")","title":"Install"},{"category":"section","location":"getting_started/#From-a-fitted-model-to-a-trajectory","page":"Getting started","text":"In production the model comes from a fitted file (SLCE.load(SLCEModel, \"model.toml\")). Here we build a small reference model through the same fitted-model surface — the classical Heisenberg ferromagnet on a simple cubic lattice, one coefficient (< 0 ⇒ ferromagnetic):\n\nusing SLCEDynamics, SLCEMonteCarlo, SLCE\nimport Spglib                      # activates SLCE's SpglibBackend extension\nusing LinearAlgebra\n\nlat = Lattice(Matrix(1.0 * I(3)))\ncell = Crystal(lat, reshape([0.0, 0.0, 0.0], 3, 1), [1], [\"Fe\"])\nspec = BasisSpec(; nbody = 2, cutoff = 1.1, lmax = [1], soc = false)\nbasis = SLCEBasis(cell, spec; backend = SpglibBackend(), images = AllImages())\nmodel = SLCEModel(basis, 0.0, [-0.01])\n\nH = TiledHamiltonian(model; dims = (2, 2, 2))       # training cell → 2×2×2 supercell\nprob = LLGProblem(H; magmom = 2.2, alpha = 0.5)     # magmom in μ_B\n\nLLGProblem resolves the LLG prefactor and the (here zero) Zeeman gradient once at construction; magmom, alpha, and g accept a scalar, a per-training-cell -atom vector, or a per-site vector.","title":"From a fitted model to a trajectory"},{"category":"section","location":"getting_started/#A-deterministic-ringdown","page":"Getting started","text":"Start from a random configuration and let the damping pull it into the ferromagnetic ground state — a deterministic run (no temperature) consumes no RNG at all:\n\nusing Random\n\nrng = Xoshiro(1)\n# random unit spins — normalized deliberately: `from_matrix` validates\n# directions and refuses a scaled column rather than normalizing it silently\nm0 = randn(rng, 3, n_sites(H))\nconfig0 = SLCEMonteCarlo.from_matrix(m0 ./ sqrt.(sum(abs2, m0; dims = 1)))\n\nres = run_llg(prob, config0; dt = 0.5, nsteps = 600,            # dt in fs\n              measure_interval = 10,\n              observables = standard_observables(H))            # MC observables plug in","title":"A deterministic ringdown"},{"category":"section","location":"getting_started/#Inspecting-the-result","page":"Getting started","text":"LLGResult carries the built-in time series (times [fs], energies — SLCE + Zeeman — and the mean spin direction over active sites), one series matrix per requested observable, and the final configuration. The damping strictly dissipates: the energy falls monotonically toward the aligned state, and the mean-spin length grows toward 1:\n\n(E_start = res.energies[1], E_end = res.energies[end],\n m_start = norm(res.mean_spins[1]), m_end = norm(res.mean_spins[end]))\n\nAny observable's series is an ncomp × n_measurements matrix aligned with res.times:\n\nres.series[:absm][:, end-3:end]        # |m| over the last four measurements","title":"Inspecting the result"},{"category":"section","location":"getting_started/#Where-to-go-next","page":"Getting started","text":"Deterministic LLG in detail — integrators, units, dt guidance, energy conservation at alpha = 0: dynamics.\nFinite temperature — the stochastic LLG, seeding, and equilibrium_stats: thermal; the semi-quantum colored-noise thermostat: quantum thermostat.\nSpectra — record a trajectory and compute S(qomega): structure factor.\nLong runs — crash-safe restart files and bit-identical resume/extension: checkpointing; the device driver: GPU.","title":"Where to go next"},{"category":"section","location":"guide/gpu/#The-GPU-path","page":"The GPU path","text":"gpu_run_llg runs the same physics on a KernelAbstractions device: both integrators, deterministic and stochastic (including the quantum thermostat), the identical Observable contract and LLGResult semantics — so equilibrium_stats, structure_factor, and checkpointing consume the result unchanged. It is exported (since the A100 go/no-go and the quantum-device smoke both passed):\n\ngH  = GPUTiledHamiltonian(backend, prob.H)     # exported by SLCEMonteCarlo; build ONCE\nres = gpu_run_llg(prob, config0, gH; dt = 0.05, nsteps = 10^6,\n                  kT = 0.02, seed = 1, checkpoint = \"llg_gpu.jld2\",\n                  checkpoint_interval = 50_000)\n\ngH is caller-built and reused across runs — the table upload is seconds at production sizes. The upstream device gradient is SLCEMonteCarlo's gpu_energy_gradient!; there is no CUDA dependency anywhere — the caller passes the backend object (e.g. CUDA.CUDABackend()).","title":"The GPU path"},{"category":"section","location":"guide/gpu/#The-determinism-contract","page":"The GPU path","text":"Same-seed CPU and GPU runs share the identical stateless Philox noise stream ((seed, site, step) is backend-free), so they are the same stochastic realization, differing only through gradient-fold and rotation roundoff. The bitwise scope differs from the CPU path:\n\nA GPU trajectory is bitwise reproducible for a fixed (seed, backend, workgroupsize, package + Julia version). The gradient fold order depends on workgroupsize (pinned default 128), so unlike the CPU path's ntasks it is part of the contract; the result's compute field and the checkpoint record all three.\nCPU↔GPU trajectories are therefore never bitwise equal — comparisons are tolerance gates on non-chaotic fixtures plus statistical equilibrium/spectral gates.\nCheckpoints written on the device resume with resume(path, prob, gH); any compute switch requires allow_compute_switch = true (see checkpointing).","title":"The determinism contract"},{"category":"section","location":"guide/gpu/#KA-CPU-as-a-test-backend","page":"The GPU path","text":"KernelAbstractions.CPU() runs the full device code path on the host — the CI gate suite compares it bitwise against a composite keyed reference, no GPU required. It is also the way to try the API locally:\n\nusing SLCEDynamics, SLCEMonteCarlo, SLCE\nimport Spglib\nusing LinearAlgebra, Random\nimport SLCEDynamics as SD    # only for the KernelAbstractions module below\n\nlat = Lattice(Matrix(1.0 * I(3)))\ncell = Crystal(lat, reshape([0.0, 0.0, 0.0], 3, 1), [1], [\"Fe\"])\nbasis = SLCEBasis(cell, BasisSpec(; nbody = 2, cutoff = 1.1, lmax = [1],\n                                 soc = false);\n                 backend = SpglibBackend(), images = AllImages())\nmodel = SLCEModel(basis, 0.0, [-0.01])\nH = TiledHamiltonian(model; dims = (2, 2, 2))\nprob = LLGProblem(H; magmom = 2.2, alpha = 0.5)\nm0 = randn(Xoshiro(1), 3, n_sites(H))\nconfig0 = SLCEMonteCarlo.from_matrix(m0 ./ sqrt.(sum(abs2, m0; dims = 1)))\n\nbackend = SD.KernelAbstractions.CPU()          # the KA module SD itself uses\ngH = GPUTiledHamiltonian(backend, H)\n\nres1 = gpu_run_llg(prob, config0, gH; dt = 0.5, nsteps = 200,\n                   kT = 0.01, seed = 9)\nres2 = gpu_run_llg(prob, config0, gH; dt = 0.5, nsteps = 200,\n                   kT = 0.01, seed = 9)\n(compute = res1.compute, repeat_bitwise = res1.energies == res2.energies)\n\nBetween measurement/checkpoint events the per-step kernel launches queue with no synchronization; the loop downloads a host snapshot only when a measurement or a checkpoint write is due, so a sparse measure_interval keeps the device busy.","title":"KA-CPU as a test backend"},{"category":"section","location":"guide/gpu/#Measured-performance-(A100)","page":"The GPU path","text":"From the decision record docs/specs/gpu-llg.md (measured 2026-07-19, kugui A100-SXM4-40GB): at the l044 8³ production bar (34,816 sites, workgroupsize = 128, dt = 0.05 fs, kT = 0.02 eV), the LLG step took 178.9 ms on the device vs 4471 ms on the same-node CPU with 8 tasks — 25.0×, clearing the ≥ 5× go/no-go. The smoke gates were green (repeat identity bitwise; same-seed CPU-vs-GPU 20-step max deviation 7.6e-16 — ULP-level). Light models are expected marginal and stay on the CPU path — no performance claims beyond these measurements.","title":"Measured performance (A100)"},{"category":"section","location":"guide/gpu/#Quantum-thermostat-on-the-device","page":"The GPU path","text":"gpu_run_llg accepts thermostat = QuantumThermostat() with the same contract as run_llg (temperature required, the same k_BTDelta thbar bounds): the stationary initialization always runs on the host and is uploaded once; the in-kernel cascade filters the same white draws as the CPU path, and the GPU resume restores quantum checkpoint files (coefficients + filter state verbatim).\n\nMeasured cost (kugui A100, bench/bench_gpu_qtb.jl on l044, records in docs/specs/quantum-thermostat.md Q4): the quantum-vs-classical step-time overhead was within timing noise at 4³ (40.8 vs 40.3 ms, 2026-07-19) and +0.4% at the 8³ production size (311.3 vs 312.6 ms, 34,816 sites, 2026-07-25) — the SLCE gradient dominates, so the thermostat choice is effectively free.","title":"Quantum thermostat on the device"},{"category":"section","location":"guide/checkpointing/#Checkpointing-and-restart","page":"Checkpointing and restart","text":"Long runs checkpoint to a JLD2 file and restart bit-identically — the resumed trajectory, measurement arrays, and final configuration equal the uninterrupted run's exactly (tested with ==, not ≈).\n\nrun_llg(prob, config0; dt = 0.5, nsteps = 10^7, kT = 0.01, seed = 1,\n        checkpoint = \"llg.jld2\", checkpoint_interval = 50_000)\n\n# …after a crash / walltime kill:\nresult = resume(\"llg.jld2\", prob)      # returns the FULL run's LLGResult\n\nWith a path, a checkpoint is written every checkpoint_interval steps (0 ⇒ only at completion) and always once at completion. Writes are atomic (temp file + mv) and consume no RNG, so checkpointing never perturbs the run it protects. One writer per checkpoint path — two concurrent runs must not share one.","title":"Checkpointing and restart"},{"category":"section","location":"guide/checkpointing/#The-resume-contract","page":"Checkpointing and restart","text":"The caller re-supplies the LLGProblem and any custom observables — function objects are not serialized. The file stores the model fingerprint (SLCEMonteCarlo.model_fingerprint), the problem parameters (magmom/alpha/g/b_ext, compared on active sites), and the observable names/component counts, and errors on any mismatch — a resume against different physics never silently continues.\nEverything trajectory-defining (dt, measure_interval, renorm_interval, integrator, kT, seed, thermostat) comes from the file and cannot be overridden.\nnsteps larger than the stored one extends the run — bit-identical to a single uninterrupted run of that length. Extension past a completed run whose final measurement was off the measure_interval grid is refused (its trace would not be a prefix of the longer run's).\nResuming a completed run reconstructs its LLGResult without stepping — resume is idempotent, so a blind \"resubmit and resume if the file exists\" retry loop is safe.\nBy default the resumed run keeps checkpointing to the same path with the stored cadence (checkpoint = nothing disables, checkpoint_interval overrides).","title":"The resume contract"},{"category":"section","location":"guide/checkpointing/#Why-the-files-are-small-and-the-restart-bitwise","page":"Checkpointing and restart","text":"The thermal noise is a stateless pure function of (seed, site, step), so no RNG state exists to store — unlike the MC checkpoints, an LLG file carries no generator words at all. The configuration is restored verbatim (deliberately not through SLCEMonteCarlo.from_matrix, whose renormalization would perturb a chaotic trajectory by ULPs), and every per-step effect — noise counter, renormalization cadence, measurement grid — is a pure function of the absolute step index.","title":"Why the files are small and the restart bitwise"},{"category":"section","location":"guide/checkpointing/#Schema-v3-contents","page":"Checkpointing and restart","text":"Plain data only (no Julia struct reconstruction), in named JLD2 groups: the schema version and kind (\"llg\"), the model fingerprint, the problem arrays, the run parameters (dt, nsteps, measure_interval, renorm_interval, integrator name, kT, seed), the compute provenance (compute/backend/workgroupsize — v2), the thermostat tag plus — for quantum runs — the filter provenance id, the authoritative filter coefficients, and the filter state restored verbatim (the one carried per-step array; v3), the completed step, the bitwise configuration, and the measurements recorded so far. v1/v2 files are back-read (as \"cpu\" / \"classical\").","title":"Schema v3 contents"},{"category":"section","location":"guide/checkpointing/#Compute-switch-policy","page":"Checkpointing and restart","text":"resume(path, prob) refuses a checkpoint written by gpu_run_llg, naming the device method; resume(path, prob, gH) continues bit-identically for the same (backend, workgroupsize) and refuses any compute switch — a CPU file, a different backend tag, or a different workgroup size — unless allow_compute_switch = true. When allowed, the continuation restores the configuration verbatim and the same noise stream, so it is the same physical realization continued exactly from a valid state — but not bit-identical to any single-backend run; the opt-in is deliberately loud. See GPU.","title":"Compute-switch policy"},{"category":"section","location":"guide/thermal/#Stochastic-LLG-(finite-temperature)","page":"Stochastic LLG (finite temperature)","text":"Passing a temperature to run_llg switches on the thermal field: per active site and step, a Gaussian gradient contribution mathbf G_mathrmthi = sigma_iboldsymbolxi_i with\n\nsigma_i = sqrtfrac2alpha_i k_B T hbar mathrmmagmom_ig_iDelta t\nquad mathrmeV\nqquad boldsymbolxi_i sim N(0 I_3)\n\nthe fluctuation–dissipation amplitude for this parametrization (no (1+alpha^2) factor — the noise rides the same prefactored equation as the deterministic field). The same draw feeds both Heun stages of one step; that is what makes the scheme converge to the Stratonovich solution. Noise requires alpha  0 on every active site — an undamped spin never thermalizes.","title":"Stochastic LLG (finite temperature)"},{"category":"section","location":"guide/thermal/#Temperature-convention","page":"Stochastic LLG (finite temperature)","text":"Exactly one of two keywords, the ecosystem-wide rule:\n\ntemperature — kelvin, converted internally with SLCE.KB_EV (assumes an eV-fitted model, the convention for DFT-fitted models);\nkT — k_B T directly in the model's energy units (theory / test runs).\n\nrun_llg takes a single scalar (no ladders — one trajectory per run).","title":"Temperature convention"},{"category":"section","location":"guide/thermal/#Seeding-and-the-stateless-noise-stream","page":"Stochastic LLG (finite temperature)","text":"Draws are keyed counter-based Philox (SLCEMonteCarlo's Random123-gated facade): a draw is a pure function of (seed, site, step). Consequences worth relying on:\n\nThe trajectory is a pure function of (prob, config0, dt, nsteps, integrator, seed) — no RNG state exists, and results are bit-identical for any ntasks.\nCheckpoints never store RNG state (see checkpointing).\nBy default every thermostatted run draws a fresh seed = rand(UInt64), recorded in LLGResult.seed, so repeated runs are independent realizations; pass an explicit seed for bit-reproducibility (tests, docs, seed ensembles).\n\nusing SLCEDynamics, SLCEMonteCarlo, SLCE\nimport Spglib\nusing LinearAlgebra, Random\n\nlat = Lattice(Matrix(1.0 * I(3)))\ncell = Crystal(lat, reshape([0.0, 0.0, 0.0], 3, 1), [1], [\"Fe\"])\nbasis = SLCEBasis(cell, BasisSpec(; nbody = 2, cutoff = 1.1, lmax = [1],\n                                 soc = false);\n                 backend = SpglibBackend(), images = AllImages())\nmodel = SLCEModel(basis, 0.0, [-0.01])\nH = TiledHamiltonian(model; dims = (2, 2, 2))\nprob = LLGProblem(H; magmom = 2.2, alpha = 0.5)\nm0 = randn(Xoshiro(1), 3, n_sites(H))\nconfig0 = SLCEMonteCarlo.from_matrix(m0 ./ sqrt.(sum(abs2, m0; dims = 1)))\n\nres = run_llg(prob, config0; dt = 0.5, nsteps = 4000, kT = 0.01, seed = 42,\n              observables = standard_observables(H))\nres2 = run_llg(prob, config0; dt = 0.5, nsteps = 4000, kT = 0.01, seed = 42,\n               ntasks = 2, observables = standard_observables(H))\nres.energies == res2.energies          # bitwise, independent of the task count","title":"Seeding and the stateless noise stream"},{"category":"section","location":"guide/thermal/#Equilibrium-statistics","page":"Stochastic LLG (finite temperature)","text":"equilibrium_stats bridges the recorded time series to SLCEMonteCarlo's binning/jackknife machinery, so the same Evaluable definitions the MC drivers accept (specific heat, susceptibility, Binder, user-defined) work on sLLG data:\n\nstats = equilibrium_stats(res)         # discards the first half by default\n(energy = stats[:energy].mean[1], err = stats[:energy].err[1],\n tau_int = stats[:energy].tau_int[1],\n specific_heat = stats[:specific_heat].mean[1])\n\nRaw observables get autocorrelation-aware means/errors (ObservableStat, via LogBinner); evaluables are jackknifed over equal-weight bins of their scalar inputs, with f(means, kT, n) receiving the run's kT and active-site count.\ndiscard drops the leading measurements as thermalization (default: the first half). The sLLG equilibration time scales like 1alpha — inspect the energy series when in doubt.\nThe default evaluables need :energy, :energy2, :m2, :m4, :absm among the recorded observables — record SLCEMonteCarlo.standard_observables(H) in the run.\n\nwarning: O(dt) equilibrium bias\nThe Heun-family sLLG has weak order 1, so equilibrium averages carry an O(dt) bias — check dt-sensitivity before trusting a tight comparison.","title":"Equilibrium statistics"},{"category":"section","location":"guide/thermal/#Cross-validation-against-Metropolis","page":"Stochastic LLG (finite temperature)","text":"The sLLG equilibrium is gated in the test suite against SLCEMonteCarlo.run_mc Metropolis averages (langle Erangle, spin correlators, at 3σ with τ_int-aware errors on both sides), and the noise parametrization is pinned by an α-independence Boltzmann gate — a wrong (1+alpha^2) would be a (1+alpha^2)times temperature error. The same comparison, live:\n\nmc = run_mc(H; kT = 0.01, sweeps_therm = 2000, sweeps_measure = 8000, seed = 7)\n(sllg = stats[:energy].mean[1], sllg_err = stats[:energy].err[1],\n metropolis = mc.points[1].stats[:energy].mean[1],\n metropolis_err = mc.points[1].stats[:energy].err[1])\n\nTwo independent samplers of the same Boltzmann ensemble — the means agree within their (τint-aware) error bars. This classical cross-check applies to the default ClassicalThermostat only; a QuantumThermostat run equilibrates to a different (non-Boltzmann) ensemble — see the [quantum thermostat](quantumthermostat.md) guide.","title":"Cross-validation against Metropolis"},{"category":"section","location":"#SLCEDynamics.jl","page":"Home","text":"Atomistic spin dynamics for fitted SLCE (spin–lattice cluster expansion) models from SLCE.jl: integrate the Landau–Lifshitz–Gilbert equation — deterministic, stochastic (classical white noise), or semi-quantum (colored-noise thermostat) — on SLCEMonteCarlo.jl's tiled supercell Hamiltonian, with equilibrium statistics that reuse the MC observable machinery, the dynamical structure factor S(qomega), bit-reproducible checkpoint/restart, and a KernelAbstractions GPU path.","title":"SLCEDynamics.jl"},{"category":"section","location":"#Where-it-sits-in-the-SLCE-family","page":"Home","text":"Package Role\nSLCE.jl fits the SLCE model (the input here)\nSLCEMonteCarlo.jl equilibrium thermodynamics: Metropolis, annealing, replica exchange\nSLCEDynamics.jl real-time dynamics: LLG / sLLG, S(qomega), QTB noise\n\nThe fitted model enters only through SLCEMonteCarlo's TiledHamiltonian and its exact all-site gradient energy_gradient! — never through SLCE internals. Observable and Evaluable definitions written for the MC drivers plug into the dynamics drivers unchanged (the same bindings are re-exported here).","title":"Where it sits in the SLCE family"},{"category":"section","location":"#Determinism-and-reproducibility","page":"Home","text":"The package follows the sibling packages' discipline:\n\nDeterministic runs consume no RNG. The fixed-step integrators are RNG-free and bit-identical for any ntasks (the only threaded piece, energy_gradient!, is itself task-count independent).\nThermal noise is stateless. Stochastic runs draw keyed counter-based Philox noise: a draw is a pure function of (seed, site, step), so trajectories are bit-reproducible for any task count and checkpoints never store RNG state.\nCheckpoints resume bit-identically — the resumed (or extended) trajectory equals the uninterrupted run's exactly, tested with ==, not ≈.\nThe scope is one package + Julia version (SLCEMonteCarlo's P6 discipline); the GPU path adds (backend, workgroupsize) to its bitwise contract.","title":"Determinism and reproducibility"},{"category":"section","location":"#Features","page":"Home","text":"LLG with per-site magmom (μ_B) / α / g, a uniform external field in tesla; energies in the model's units (eV), time in fs.\nIntegrators: Depondt–Mertens (rotation-based Heun, norm-exact — the default) and projected Heun (an independent cross-check).\nStochastic LLG validated against analytic Boltzmann distributions and SLCEMonteCarlo Metropolis equilibrium; equilibrium_stats reuses the MC binning/jackknife machinery.\nA semi-quantum colored-noise thermostat (QuantumThermostat) — magnon occupations follow Bose–Einstein statistics instead of classical equipartition.\nS(qomega): the full Hermitian tensor on a two-sided frequency axis, with the elastic tensor separated, Welch averaging, and seed ensembles — conventions pinned by exact analytic gates.\nCrash-safe JLD2 checkpoint/restart, and a GPU driver (gpu_run_llg) with the same noise stream and result semantics as the CPU path.","title":"Features"},{"category":"section","location":"#Reading-order","page":"Home","text":"Getting started → the guides (dynamics, thermal, quantum thermostat, structure factor, checkpointing, GPU) → the API reference. Design decision records live in docs/specs/ inside the repository.","title":"Reading order"},{"category":"section","location":"guide/structure_factor/#The-dynamical-structure-factor-S(q,ω)","page":"The dynamical structure factor S(q,ω)","text":"structure_factor estimates the classical dynamical spin structure factor from a recorded trajectory. The estimator (all signs and normalizations pinned by exact analytic gates — Larmor, dimer, ring dispersion, Parseval sum rules):\n\nbeginaligned\ns^alpha(qt)          = tfrac1sqrt N sum_smathrmactive\n                          e^-2pi i fcdot x_s(e_s^alpha(t) - bar e_s^alpha)\nS^alphabeta(qomega_k) = tfracDelta t_sM W_2\n                          X^alpha(qomega_k)^* X^beta(qomega_k)\nendaligned\n\nwith f the fractional q (training-cell r.l.u.), x_s the fractional site position, bar e_s the per-site time mean over the analysis window, and X the windowed DFT of s. A spin precessing positively about +hat z lands at +omega; a magnon running toward +q lands at +q.","title":"The dynamical structure factor S(q,ω)"},{"category":"section","location":"guide/structure_factor/#Recording-a-trajectory","page":"The dynamical structure factor S(q,ω)","text":"The trajectory is an ordinary observable — trajectory_observable(H) records the full configuration (ncomp = 3·n_sites), so it inherits the measurement cadence, checkpoint persistence, resume/extension, and bit-reproducibility with zero new machinery. Memory guidance: the in-RAM trajectory is 24·n_sites·n_meas bytes — keep it ≲ 10 GB (e.g. 8³ cells × 68 atoms × 4096 frames ≈ 3.4 GB). trajectory unpacks the series as a zero-copy 3 × n_sites × n_meas array (dropping an off-grid final measurement — the estimator needs a strictly uniform time grid).","title":"Recording a trajectory"},{"category":"section","location":"guide/structure_factor/#q-points-and-paths","page":"The dynamical structure factor S(q,ω)","text":"q is fractional, in the training-cell reciprocal lattice; only supercell-commensurate points (f_i N_i in mathbb Z) are representable. structure_factor throws on incommensurate points, naming the nearest representable one; q_path(crystal, vertices; npoints, dims = H.dims) snaps loudly — it returns both the snapped qs and the pre-snap qs_requested, so the snapping is always visible.","title":"q points and paths"},{"category":"section","location":"guide/structure_factor/#A-worked-example:-magnons-on-a-spin-chain","page":"The dynamical structure factor S(q,ω)","text":"An 8-cell tiling of a ferromagnetic chain along x (the cell is elongated in y/z, so only the pm x neighbors are within the cutoff). Thermal fluctuations populate every magnon mode; light damping keeps the modes sharp:\n\nusing SLCEDynamics, SLCEMonteCarlo, SLCE\nimport Spglib\nusing LinearAlgebra, Random\n\nlat = Lattice(Matrix(Diagonal([1.0, 4.0, 4.0])))    # a chain: only ±x in range\ncell = Crystal(lat, reshape([0.0, 0.0, 0.0], 3, 1), [1], [\"Fe\"])\nbasis = SLCEBasis(cell, BasisSpec(; nbody = 2, cutoff = 1.1, lmax = [1],\n                                 soc = false);\n                 backend = SpglibBackend(), images = AllImages())\nmodel = SLCEModel(basis, 0.0, [-0.01])\nH = TiledHamiltonian(model; dims = (8, 1, 1))       # 8 sites along the chain\nprob = LLGProblem(H; magmom = 2.2, alpha = 0.05)\nconfig0 = SLCEMonteCarlo.from_matrix(repeat([0.0, 0.0, 1.0], 1, n_sites(H)))\n\nres = run_llg(prob, config0; dt = 1.0, nsteps = 4096, kT = 0.002, seed = 5,\n              measure_interval = 4,\n              observables = [trajectory_observable(H)])\n\npath = q_path(cell, [[0.0, 0, 0], [0.5, 0, 0]]; npoints = 5, dims = H.dims)\nsqw = structure_factor(res, H, cell, path.qs; discard = 256)\n\nThe transverse channel S^+- resolves the magnon circulation sense — for a ferromagnet ordered along +hat z, all one-magnon weight sits at positive ω, rising from the Goldstone mode at Γ:\n\nspm = sqw_plusminus(sqw)                       # nq × nω, real ≥ 0\npos = findall(>(0), sqw.omegas)                # the positive-ω half\npeaks = [homega_mev(sqw)[pos[argmax(spm[iq, pos])]] for iq = 2:length(path.qs)]\n(q = [Tuple(q) for q in path.qs[2:end]], peak_hw_mev = round.(peaks; digits = 2))","title":"A worked example: magnons on a spin chain"},{"category":"section","location":"guide/structure_factor/#Estimator-controls","page":"The dynamical structure factor S(q,ω)","text":"Welch averaging: nsegments segments of derived power-of-two length nfft (seglength overrides; trailing samples are truncated, never zero-padded), with fractional overlap and a :hann (default) or :none window.\ndiscard drops leading measurements as thermalization.\nMean/elastic separation: the per-site time mean over the analysis window (one global mean, never per segment) is always subtracted; the elastic tensor is reported separately in S_el, so Bragg weight cannot leak through the window into the inelastic spectrum.\nThe result is bit-identical for any ntasks and across repeated calls.\n\nnote: Windowing caveat\nHann windowing plus mean subtraction still leaves a residual low-ω leak on thermal data — inspect the near-elastic bins accordingly.","title":"Estimator controls"},{"category":"section","location":"guide/structure_factor/#The-result-and-its-reductions","page":"The dynamical structure factor S(q,ω)","text":"SQWResult stores the full 3×3 Hermitian tensor S[α, β, iq, iω] on a two-sided fftshifted frequency axis (omegas [rad/fs]; for even nfft the Nyquist bin exists only on the negative side), plus the elastic tensor and the estimation metadata. Reductions are cheap post-contractions:\n\nsqw_diag — the three diagonal components;\nsqw_trace — sum_alpha S^alphaalpha, the rotation-invariant total intensity (the sum-rule object);\nsqw_perp — the unpolarized-neutron weight (NaN at Γ, where the direction is undefined — no silent convention);\nsqw_plusminus — the ω-sign-resolving transverse channel;\nsqw_elastic — the elastic tensor.\n\nAxis helpers: homega_ev / homega_mev (hbaromega) and freq_thz.","title":"The result and its reductions"},{"category":"section","location":"guide/structure_factor/#Ensembles-and-files","page":"The dynamical structure factor S(q,ω)","text":"structure_factor(::Vector{LLGResult}, …) averages a seed ensemble (spectra, never amplitudes) and reports per-element realization standard errors in err for ≥ 3 members; members must share dt, nsteps, and measure_interval. structure_factor(path::AbstractString, …) reads a run_llg checkpoint file directly — a completed checkpoint is the persisted trajectory format.","title":"Ensembles and files"},{"category":"section","location":"guide/structure_factor/#Verification:-the-channel-sum-rule","page":"The dynamical structure factor S(q,ω)","text":"channel_sumrule (public, unexported) checks the global Parseval identity of the estimator at the sublattice-channel level against the direct-space sum_smathrmactive (1 - bar e_s^2) — a plumbing gate valid for any trajectory:\n\ntr = trajectory(res)\nSLCEDynamics.channel_sumrule(tr.traj, tr.times, H, cell)","title":"Verification: the channel sum rule"}]
}
